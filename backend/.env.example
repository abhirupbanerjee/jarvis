# ── Gemini API Key (REAL key — NEVER ships to client) ──
GEMINI_API_KEY=

# ── Model ──
GEMINI_MODEL_ID=gemini-2.5-flash-native-audio-latest

# ── Shared secret for app-to-backend auth ──
# Generate with: openssl rand -hex 32
TOKEN_SHARED_SECRET=

# ── Server port ──
PORT=8080
